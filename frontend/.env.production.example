# Frontend Environment Variables for Production
# Copy this file to .env.production and update the values
# Or set these in docker-compose.yml or your deployment platform

# Backend API Base URL
# Update this with your production backend URL
NEXT_PUBLIC_API_BASE_URL=https://agentapi.dosibridge.com

# Note: Only variables prefixed with NEXT_PUBLIC_ are available in the browser
# Other variables are only available on the server side (API routes, server components)
