# Database Configuration
DATABASE_URL="postgresql://user:password@localhost:5432/mcpagent"

# JWT Authentication Secret Key (Still used for legacy/internal signing if needed, but Auth0 handles main auth)
# Generate with: python3 backend/generate_keys.py
JWT_SECRET_KEY=your-generated-jwt-secret-key-here

# MCP API Key Encryption Key
MCP_APIKEY_ENCRYPTION_KEY=your-generated-encryption-key-here

# CORS Origins (comma-separated)
CORS_ORIGINS="http://localhost:3000,http://localhost:8086"

# LLM API Keys (optional, can be set via UI)
OPENAI_API_KEY=""
GOOGLE_API_KEY=""
GROQ_API_KEY=""

# Environment
ENVIRONMENT="development"
FRONTEND_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# AUTH0 CONFIGURATION (REQUIRED)
# -----------------------------------------------------------------------------
# 1. Create an account at https://auth0.com/
# 2. Create a "Single Page Web Application"
# 3. Configure Allowed Callback/Logout URLs to http://localhost:3000
# 4. Copy the values below from your Auth0 Application Settings
# -----------------------------------------------------------------------------

# Backend (.env) & Frontend (.env.local) share these:
AUTH0_DOMAIN=your-tenant.us.auth0.com
AUTH0_CLIENT_ID=your-client-id
AUTH0_AUDIENCE=https://dosibridge-api

# NOTE: For Frontend, you must also add these to frontend/.env.local (or .env) with NEXT_PUBLIC_ prefix:
# NEXT_PUBLIC_AUTH0_DOMAIN=your-tenant.us.auth0.com
# NEXT_PUBLIC_AUTH0_CLIENT_ID=your-client-id
# NEXT_PUBLIC_AUTH0_AUDIENCE=https://dosibridge-api

# Email/SMTP Configuration (Optional, handled by Auth0 for auth emails usually)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM_EMAIL=""
CONTACT_EMAIL=""
